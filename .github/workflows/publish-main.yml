name: Publish main branch

on:
  push:
    branches:
      - main

permissions:
  id-token: write  # Required for OIDC https://docs.npmjs.com/trusted-publishers
  contents: write  # Required for pushing tags

jobs:
  publish-main:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # npm@latest refuses to install on Node 20 since npm 12 ("Required: ^22.22.2 ||
          # ^24.15.0 || >=26.0.0"), which failed the publish outright.
          node-version: 24
          registry-url: https://registry.npmjs.org/
      # Ensure npm 11.5.1 or later is installed, as trusted publishing (OIDC) needs it
      - run: npm install -g npm@latest
      - name: Bump version
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          npm version patch
      - name: Publish to NPM
        run: npm publish --access public
      - name: Commit and push
        run: |
          git push --follow-tags
